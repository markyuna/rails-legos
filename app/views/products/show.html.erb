<div class="cover mb-4 d-flex align-items-center justify-content-center">
  <div class="text-center text-white">
   <h1 class="display-1 "><%= @product.title %></h1>
  </div>
</div>
<div class="container">
  <div class="cards mb-3" >
  <div class="row g-0">
    <div class="col-md-4">
      <% if @product.photo.attached? %>
          <%= cl_image_tag @product.photo.key, class: 'card-img-top my_product_image' %>
      <% end %>
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h1 class="title-inner"><%= @product.title %></h1><br>
        <p class="card-content"><%= @product.description %></p><br>
        <p class="card-content"><%= @product.price_per_day %>€/jour </p>
      </div>
    </div>
  </div>
</div>
<div class="text-decoration-none text-center">
    <%= link_to "Louer", "/products/#{params[:id]}/bookings/new", class: "btn btn-outline-primary" %>
    <%= link_to "Retour aux produits", products_path, class: "btn btn-outline-primary" %>
</div>
</div>
<br>
<br>
